__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints
.pytest_cache/

# Raw candidate pool — large, supplied by organizers at reproduction time.
input/candidates.jsonl

# BM25 index — ~120 MB, exceeds GitHub's 100 MB file limit. It is regenerable:
# precompute.py rebuilds it, and rank.py auto-builds it from --candidates if absent.
# The LTR artifacts that ARE committed (features.parquet, ltr_model.txt,
# feature_names.json) are enough for rank.py to reproduce the submitted ranking.
input/bm25_index.pkl

# NOTE: output/submission.csv IS committed (the final ranking) — do not ignore it.
